# 🚀 최적화된 크롤링 서비스 의존성 (2025-09-02)
# 50% 메모리 절약, 3배 성능 향상 목표

# === 핵심 웹 프레임워크 ===
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0

# === 설정 관리 ===
pyyaml>=6.0.0

# === Tier 1: httpx (경량 HTTP 클라이언트) - 90%+ 요청 처리 ===
httpx>=0.25.0

# === Tier 2: BeautifulSoup (정적 HTML 파싱) - 5-8% 요청 처리 ===
beautifulsoup4>=4.12.0
lxml>=4.9.0  # 고성능 XML/HTML 파서

# === Tier 3: Playwright (동적 콘텐츠 전용) - 2-5% 요청 처리 ===
playwright>=1.40.0  # 필요시에만 설치

# === 데이터베이스 ===  
asyncpg>=0.28.0

# === 모니터링 & 성능 ===
psutil>=5.9.0  # 메모리 사용량 모니터링

# === 제거된 무거운 패키지들 ===
# selenium>=4.15.0  # 🚫 제거: 600MB+ 메모리 사용
# undetected-chromedriver>=3.5.0  # 🚫 제거: Selenium 의존성
# aiohttp>=3.8.0  # 🚫 제거: httpx로 대체
# webdriver-manager  # 🚫 제거: 브라우저 드라이버 관리 불필요
# chromedriver-binary  # 🚫 제거: 바이너리 패키지 불필요

# === 예상 성능 개선 ===
# 메모리 사용량: 1.3GB → 650MB (50% 절약)
# 응답 속도: 5.3초 → 1.8초 (3배 향상)  
# 동시 처리: 10개 → 50개 (5배 증가)
# 패키지 크기: ~800MB → ~200MB (75% 절약)